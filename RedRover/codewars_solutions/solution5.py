def main(verb, noun):
    # This function has three problems: square brackets instead of parenthesis,
    # a colon after the parenthesis and the return command inside the
    # function is not indented.
    # not annotated type is not checked
    return verb + noun
